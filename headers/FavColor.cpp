#include "FavColor.h"

const char* FavColor(char Color){
    if(Color == 'r'){
        return "apple\n";
    }
    else if(Color == 'y'){
        return "lemon\n";
    }
    else return "nothing\n";
}

